--!strict

local CombatTypes = require(script.Parent.Parent.types.CombatTypes)

-- All numbers in this file are working tuning values, not final balance.
local definitions: { [string]: CombatTypes.MobDefinition } = {
	young_wolf = {
		id = "mob_young_wolf",
		displayName = "Молодой волк",
		level = 1,
		maxHealth = 70,
		rewardXP = 15,
		lootTableId = "loot_young_wolf",
		respawnSeconds = 5,
		aggressionMode = "Passive",
		rank = "Ordinary",
		attackKind = "Melee",
		detectionRadius = 25,
		aggroRadius = 20,
		reacquireRadius = 25,
		leashDistance = 60,
		attackRange = 10,
		basicAttackDamage = 8,
		attackWindupSeconds = 0.25,
		attackIntervalSeconds = 1.6,
		chaseSpeed = 12,
		decisionIntervalSeconds = 0.25,
		restoreHealthOnReturn = true,
	},
	grey_wolf = {
		-- Preserve the Milestone 0 key and id while extending its data contract.
		id = "mob_grey_wolf",
		displayName = "Волк",
		level = 2,
		maxHealth = 100,
		rewardXP = 25,
		lootTableId = "loot_grey_wolf",
		respawnSeconds = 5,
		aggressionMode = "Aggressive",
		rank = "Ordinary",
		attackKind = "Melee",
		detectionRadius = 35,
		aggroRadius = 30,
		reacquireRadius = 40,
		leashDistance = 75,
		attackRange = 10,
		basicAttackDamage = 22,
		attackWindupSeconds = 0.25,
		attackIntervalSeconds = 1.5,
		chaseSpeed = 14,
		decisionIntervalSeconds = 0.25,
		restoreHealthOnReturn = true,
	},
	spider = {
		id = "mob_spider",
		displayName = "Паук",
		level = 3,
		maxHealth = 85,
		rewardXP = 35,
		lootTableId = "loot_spider",
		respawnSeconds = 5,
		aggressionMode = "Aggressive",
		rank = "Ordinary",
		attackKind = "Ranged",
		detectionRadius = 30,
		aggroRadius = 25,
		reacquireRadius = 35,
		leashDistance = 70,
		attackRange = 35,
		basicAttackDamage = 10,
		attackWindupSeconds = 0.35,
		attackIntervalSeconds = 1.8,
		chaseSpeed = 11,
		decisionIntervalSeconds = 0.3,
		restoreHealthOnReturn = true,
	},
}

return table.freeze(definitions)
